<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>School Record System</title>
    <link rel="stylesheet" href="/styles/index.css">
    <link rel="stylesheet" href="/styles/utility.css">
</head>
<body>
    <div class="main-container">
        <div class="userinfo">
            <div class="info">
                <h1 ><%= teacherInfo.firstname.charAt(0).toUpperCase() + teacherInfo.firstname.slice(1)+ " " + teacherInfo.lastname.charAt(0).toUpperCase() + teacherInfo.lastname.slice(1)%></h1>
                <h3 ><%= teacherInfo.subject.charAt(0).toUpperCase() + teacherInfo.subject.slice(1) %></h3>
                <h3 ><%= teacherInfo.contactnum %></h3>
                <h3 ><%= teacherInfo.emailid %></h3>
                <h3 ><%= teacherInfo.schoolname.charAt(0).toUpperCase() + teacherInfo.schoolname.slice(1) %></h3>
                <form action="/upload-teacher-profile" method="post" enctype="multipart/form-data">
                    <label for="profileInput" >Upload Profile Image</label>
                    <input  class="disp-none" type="file" name="userProfile" id="profileInput" >
                     <button type="submit" id="changePfp" class="upload-btn disp-none">Upload</button>
                </form>
                <script src="/scripts/pfp-upload.js"></script>
            </div>
            <div class="img-cont">
               <img src="/images/userimg.png" alt="">
            </div>
        </div>
        <div class="options">
            <div class="opt-cards card">

                <img src="/images/mark attendence.webp" alt="mark attendence">
                <button class="opt-btn"><span>Mark</span> <span>Attendence</span></button>
            </div>
            <div class="opt-cards card">

                <img src="/images/see atendence.png" alt="see attendence">
                <button class="opt-btn"><span>View</span> <span>Attendence</span></button>
            </div>
            <div class="opt-cards card">
                <img src="/images/give homework.png" alt="see homework">
                <button class="opt-btn"><span>Give</span> <span>Homework</span></button>
            </div>
            <div class="opt-cards card">
                <img src="/images/give test score.jpg" alt="view testscore">
                <button class="opt-btn"><span>Give</span> <span>Testscores</span></button>
            </div>
        </div>
    </div>
</body>
</html>